@page "{id:int?}"
@model RPInventories.Pages.Brands.DetailsModel

@{
    ViewData["Title"] = "Brand Details";
}

<h4>@ViewData["Title"]</h4>
<hr />
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Brand.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Brand.Name)
        </dd>
    </dl>
</div>
<div class="row">
    <div class="col">
        <partial name="Partials/Actions/_Details" model="@Model.Brand.Id"/>
    </div>
</div>
